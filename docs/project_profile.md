# 项目经历描述：电力 SCADA 数据采集与监控系统

项目采用 C++17 + Socket 实现主站侧 IEC60870-5-104 前置采集程序，模拟通过调度数据网接入子站远动装置，完成遥测、遥信、状态监控、告警和本地数据缓存。系统按照电网远动系统现场联调流程设计，重点验证通信稳定性、协议解析正确性、异常恢复和测点告警处理。

## 项目职责

- 负责主站/子站通信链路设计，实现 IEC60870-5-104 TCP 客户端接入、STARTDT 启动流程、I 帧解析、S 帧确认和 U 帧控制响应。
- 负责远动测点模型设计，按 IOA 建立遥测、遥信点表，支持阈值告警、遥信异常、质量位异常和数据无刷新告警。
- 负责通信异常处理，覆盖掉线重连、连接超时、接收超时、帧头错位、长度异常、未知 TypeID 和数据质量异常。
- 负责现场联调模拟环境，开发 IEC104 子站模拟服务端，支持周期上送遥测遥信、主动断链、越限值和遥信变位，用于验证主站侧稳定性。
- 负责本地缓存机制，将测量值和告警事件落盘，网络抖动或链路中断时保证运行过程可追溯。

## 协议与工程细节

- U 帧流程：主站连接子站后发送 `STARTDT act`，收到 `STARTDT con` 后进入数据传输；收到 `TESTFR act` 时回复 `TESTFR con`，维持链路可用性。
- I 帧流程：按 APCI 控制域解析发送序号和接收序号，按 ASDU 提取 TypeID、VSQ、COT、公共地址、IOA 和信息体。
- S 帧流程：主站收到 I 帧后按发送序号递增回送 S 帧确认，避免子站侧认为主站未确认数据。
- 遥测解析：支持 `M_ME_NC_1` 短浮点和 `M_ME_NB_1` 标度值，解析 QDS 中 invalid、not topical、substituted、blocked、overflow 等质量位。
- 遥信解析：支持 `M_SP_NA_1` 单点遥信，解析 SIQ 状态位和质量位，处理开关变位、异常状态和恢复事件。

## 现场联调口径

联调过程按真实远动接入思路推进：先核对子站 IP、端口、公共地址和 IOA 点表，再抓取主站启动流程是否完成 STARTDT；随后通过模拟子站注入越限值、遥信变位和链路闪断，观察主站日志、告警恢复、缓存落盘和自动重连。对于数据错乱场景，接收端从 `0x68` 重新同步帧头，并在长度或格式异常时释放连接，避免错误帧污染后续测点。

## 工程成果

- 完成模拟主站/子站环境下 IEC60870-5-104 采集链路闭环，覆盖连接、启动、采集、确认、告警、缓存和重连。
- 按 2,000 点测点规模设计点表和缓存机制，默认缓存 10,000 条记录，支持现场按站点容量扩展。
- 支持 1 秒级采集周期，模拟环境下数据从子站上送到主站解析入缓存为毫秒级处理。
- 默认掉线 2 秒后自动重连，链路恢复后继续采集，主站进程无需人工重启。
- 告警覆盖遥测越限、遥信异常、质量位异常、数据超时，便于调度运行监视和故障追溯。

